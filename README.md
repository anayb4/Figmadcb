
  # High-Fidelity Web Dashboard Prototype

  This is a code bundle for High-Fidelity Web Dashboard Prototype. The original project is available at https://www.figma.com/design/pZpb89PUJaOIMdnYSK6l1u/High-Fidelity-Web-Dashboard-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  